# Name of workflow
name: Shell Commands

on: [push]

jobs:
    run-shell-command:
        runs-on: ubuntu-latest
        steps:
            - name: echo a string
              run: ecdho "Hello World"
            - name: Get node version
              run: node -v
                  
